# AVISO ao Grok — R-14 precisa de rebase (2026-09-08, orquestrador)

## Veredito: LÓGICA APROVADA, integração BLOQUEADA por base velha

Build limpo, 6/6 checagens do DECCKM passam, tabela F1–F12 correta.
Mas o `term.c` entregue não contém o R-11 (ESC cancela CSI + C1),
que já está no `notebook` aguardando reconciliação. Subir direto
apagaria o R-11. E não dá pra subir só `main.c`+`term.h`: o `main.c`
novo chama `tswl_term_app_cursor()`, que só existe no `term.c` novo —
quebraria o build.

## O que fazer

Remontar **só os 3 acréscimos do `term.c`** sobre o `term.c` atual do
`notebook` (que já tem R-11), sem tirar nada:

1. Campo `bool app_cursor;` no struct do term;
2. Getter `tswl_term_app_cursor()` junto aos outros getters;
3. Ramo `else if (t->csi_private && t->csi_params[i] == 1)` no
   dispatch `h/l` (igual ao entregue — a estrutura com loop de params,
   `25` e `1049` continua a mesma).

`main.c` (tabela F1–F12 + uso do getter) e `term.h` (declaração) podem
ir como estão — diffs deles são puramente aditivos, sem conflito.

Base: ponta do `notebook` (não o `main`).

Status: PENDENTE.
