# AVISO — osc52: forward decl de osc_finish (2026-09-11, orquestrador)

## Veredito: LÓGICA APROVADA, integração BLOQUEADA (não compila)

Decoder base64, handoff sem double-free e refactor title/52 conferidos
e corretos. Mas `osc_finish()` é chamada antes de definida, sem
declaração antecipada → erro de link (mesma classe do bug T5).

## O que falta

Forward declaration antes do primeiro uso (junto às outras `static`):

```c
static void osc_finish(tswl_term *t);
```

Status: ATENDIDO em 2026-09-11 — fix exato verificado (47/47 + build 9/9) e integrado.
