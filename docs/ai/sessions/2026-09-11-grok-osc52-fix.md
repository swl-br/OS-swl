# Sessao — 2026-09-11

IA: Grok
Responsavel: fix aviso-osc52 (forward decl osc_finish)

Base: main/notebook atual (CSI3J, bracketed, select_all, bell, W1)

## Fix
```c
static void osc_finish(tswl_term *t);
static char *b64_decode(...);
```
antes do primeiro uso (junto a blank_cell).

OSC 52 completo + take_clipboard + main handoff + teste aGk= → "hi".
