; demo.swl -- minimal SWL program: functions, calls, arithmetic
module main

fn add(x: i32, y: i32) -> i32
    return x + y
end

fn main() -> i32
    var a: i32 = 2
    var b: i32 = 3
    var r: i32 = add(a, b)
    swl_print_i32(r)
    return r
end
